{
  "name": "neo4j-dataflow-visualizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/ui/events.js src/styles/index.css --bundle --outdir=public --format=iife --platform=browser --entry-names=bundle",
    "test": "vitest --environment jsdom"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "jsdom": "^24.1.0",
    "vitest": "^2.1.1"
  }
}
